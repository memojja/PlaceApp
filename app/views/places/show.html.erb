
shoW<br /><br />

<b>ID:  </b><%= @place.id %><br />
<b>Title:  </b><%= @place.name %><br />
<b>Description:  </b><%= @place.description %><br />
<b>Category: </b><%= @place.category.name %>
<b>Comments :</b>

  <% @place.comments.each do |comment| %>


      <p>
        <strong>Comment:</strong>
        <%= comment.body %><%= comment.customer.email %>
      </p>
  <% end %>

<br/>

<% if current_customer.present? %>
<%= form_for([@place , @place.comments.build]) do |c| %>

    <%= c.text_area :body, placeholder: "body", rows: 10 %> <br>

    <%= c.submit 'Gonder' %>

  <% end %>

<br/>
<% end %>
  <% if current_owner.present? and current_owner.id == @place.owner_id %>


<%= link_to "Back to place list", places_path %>
<br/>
<%= link_to "Edit place", edit_place_path(@place) %>
<br/>
<%= link_to "Sil", place_path(@place), method: :delete %>

  <% end %>

